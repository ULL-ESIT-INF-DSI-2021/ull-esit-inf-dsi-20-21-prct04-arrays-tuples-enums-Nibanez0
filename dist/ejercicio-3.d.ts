declare function meanAndConcatenate(array: (number | string[]), [number, string]: [any, any], { let, numeros: number, let, media: number, let, cadena: string, array, forEach }: {
    let: any;
    numeros?: number;
    media?: number;
    cadena?: string;
    array: any;
    forEach: any;
}): any;
